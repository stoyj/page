//🧱 Какво правим във всеки файл?

//✅ 1. HelloWorld.js – правим компонент, който казва “Hello World!”:
//✅ 2. HelloWorld.js – правим текста цветен и сменяем.

// Супер! 😄 Хайде да направим твоята "Hello World!" страничка по-цветна и по-забавна, като добавим: 🎨 Цветен текст  🖱️ Бутон, който сменя текста при натискане.

//📁Ще работим с трите файла: HelloWorld.js,  App.js, index.js (не го пипаме — остава същият).

// import React from "react"; // Нужно, за да използваме JSX
import React, { useState } from "react";

function HelloWorld() {
  const [text, setText] = useState("Hello World!"); // Създаваме "text"
  const [color, setColor] = useState("blue"); // Създаваме "color"

  const changeText = () => {
    setText("Здрасти от React! 😊");
    setColor("green"); // Сменяме цвета
  };

  return (
    <div>
      <h1 style={{ color: color }}>{text}</h1>
      <button onClick={() => setColor("red")}>Червено</button>
      <button onClick={() => setColor("blue")}>Синьо</button>
      <button onClick={() => setColor("purple")}>Лилаво</button>
    </div>
  );
}

export default HelloWorld; // Казваме: „Можеш да използваш този компонент и в други стаи“

// В кой files трябва да напиша кода обърках се
